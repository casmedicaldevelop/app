// Scenario SM-1: Form renders all fields: product (required), cum (optional), price (required)
// Scenario SM-2: Submitting with valid data creates the record and navigates to list
// Scenario SM-3: Submitting with empty required fields (product, price) shows validation errors
// Scenario SM-4: price field rejects values below 0
// Scenario SM-5: Cancel button navigates back to stop-max list without saving
// Scenario SM-6: API error is shown inline below the form header

export {}
