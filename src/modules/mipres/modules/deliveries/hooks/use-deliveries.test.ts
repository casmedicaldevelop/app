// Escenarios observables — useDeliveries.
// RP-1  expone deliveryTotals (Map IDEntrega→total_price) y totalsReady, consultados por prescripción.
// RP-2  reportDelivery manda valorEntregado = total precargado y deliveryId = String(item.IDEntrega).
export {}
