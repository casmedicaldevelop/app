// Escenarios observables — deliveriesService.
// deliveryTotals(prescriptionNumber): GET /filing-mipres/delivery-totals → [{ deliveryId, totalPrice }].
// createReport ahora incluye deliveryId (IDEntrega) además de miPresEntregaId y valorEntregado.
export {}
