// Escenarios observables — InlineFacturacionForm.
// FP-3  se ocultan los inputs de ID direccionamiento y valor unitario; ambos vienen del radicado por IDReporteEntrega.
// FP-4  el formulario pide únicamente el código de factura; se habilita solo si hay prefill (routingId + unitPrice).
export {}
