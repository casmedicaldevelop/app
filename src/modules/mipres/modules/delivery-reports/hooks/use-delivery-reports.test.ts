// Escenarios observables — useDeliveryReports.
// FP-1  expone facturacionPrefill (Map IDReporteEntrega → { routingId, unitPrice }) y prefillReady.
// FP-2  createFacturacion incluye deliveryReportId para que el backend actualice billing_id + invoice_code.
export {}
