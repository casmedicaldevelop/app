// Escenario observable — schedulesService.deliveredScheduleIds(prescriptionNumber)
// GET /filing-mipres/delivered-schedule-ids?prescriptionNumber=N → string[] de schedule_id con entregas.
export {}
